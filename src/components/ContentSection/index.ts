import StyledContentSection from './StyledContentSection';
export default StyledContentSection;
/* i did not make a content section component
that uses the styled component as a wrapper because
i need to put content inside of the styled
component which isnt allowed if i put
another wrapper over this component*/
